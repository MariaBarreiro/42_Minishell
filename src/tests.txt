gdb tui

TESTS:
-------------------------------------------------------------------------------------------

-> basic redirect:
	echo "abc" > a.txt
	a.txt < grep "a"

	echo "def" >> a.txt

	grep a << EOF 


ERRORS:
-------------------------------------------------------------------------------------------

typedef struct s_cmd_block
{
	int					redir_in;
	int					heredoc;
	char				**limits;
	char				**args;
	char				**input;
	int					n_outputs;
	t_output			*outputs;
	struct s_cmd_block	*next;
} t_cmd_block;
