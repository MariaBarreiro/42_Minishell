/* t_output	*populate_output()
{
	t_output	*head;

	head = malloc(sizeof(t_output));
	if (!head)
		return (NULL);
	head->file = ft_strdup("a.txt");
	head->append = 1;
	head->next = NULL;
	return (head);
} */